# Native Agent resources

`build-standalone.ps1` stages the current Release x64 Native DLL and its three
read-only TSV tables into this directory before Cargo or Tauri packaging. The
generated files are intentionally not committed.
